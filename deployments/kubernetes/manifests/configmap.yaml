---
# Source: Chowkidar/templates/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  annotations:
    fabric8.io/target-platform: kubernetes
  labels:
    app: chowkidar
    group: com.stakater.platform
    provider: stakater
    version: 0.0.6
    chart: "Chowkidar-0.0.6"
    release: "RELEASE-NAME"
    heritage: "Tiller"
  name: chowkidar
data:
  config.yaml: |-
    controllers:
    - type: pods
      watchcriterion: 
        operator: 
        identifiers:
        - resourceExists
        - healthCheckExists
      actions:
      - name: slack
        params:
          channel: channel-name
          token: your-token
