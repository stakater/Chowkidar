---
# Source: Chowkidar/templates/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  annotations:
    fabric8.io/target-platform: kubernetes
  labels:
    app: chowkidar
    group: com.stakater.platform
    provider: stakater
    version: 
    chart: "Chowkidar-0.0.1"
    release: "RELEASE-NAME"
    heritage: "Tiller"
  name: chowkidar
data:
  config.yaml: |-
    controllers:
    - type: pods
      watchcriterion: 
        operator: 
        identifiers:
        - resourceExists
        - healthCheckExists
      actions:
      - name: slack
        params: 
          token: 
          channel: 
